from setuptools import setup

setup(
    name='django-schema-csv',
    description='A Django app providing a management command to dump your DB schema as CSV',
    packages=['schemacsv'],
    platforms='any',
)
